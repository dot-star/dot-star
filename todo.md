# todo

- Automate permission triage: promote recurring requests from `~/.claude/permission-requests.jsonl` into the global allowlist without prompting. Replaces the removed `triage_nudge.sh` SessionStart nudge and `triage-permissions` skill (commit `90ffb8c`); ideally runs as a background agent off the logging ledger.
- Format and type-check Python with the ruff toolchain: replace the `black` pre-commit hook with `ruff-format` (`ruff-check` is already wired), select real lint rules under `[tool.ruff.lint]` in `pyproject.toml` starting with the annotation rules, and add a pinned `ty` hook scoped to the checked paths.
- Drop the blanket `Bash(gh api:*)` allow rule in `ai/files/Users/user/.claude/settings.json` and add a `gh api` read safe-list to `hooks/validate_bash_command.sh`; the `ask` list only names today's write flags, so a write flag it doesn't name is permitted silently instead of prompting.
- Pin or delete the four `Bash(git -C * ...)` allow rules in `ai/files/Users/user/.claude/settings.json`; the wildcard sits before the subcommand, so each rule also matches smuggled options such as `-c core.pager=<command>` (Claude Code warns about this shape at startup).
- Remove the waiver mechanism from `ai/files/Users/user/.claude/settings_test.py` once those two land: delete `WAIVED_ALLOW_RULES`, the `waived` keys in `settings_test_cases.json`, and the two WAIVE branches, so every remaining check fails the suite outright.
