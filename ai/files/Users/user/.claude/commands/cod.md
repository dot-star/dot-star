---
description: Promote a saved memory into a mechanism that fires on its own (hook, `settings.json` entry, `CLAUDE_*.md` line, alias, or skill).
---

Codify the memory under discussion: invoke the `codify` skill.
