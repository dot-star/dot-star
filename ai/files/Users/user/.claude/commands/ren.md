---
description: Draft a short, descriptive title for this session and copy a ready-to-paste `/rename <title>` line to the clipboard.
---

Give me a name for this session: a short, descriptive title (a brief plain-language description, not a slug), with a relevant PR appended if one exists. Prefix it with `/rename ` and copy the line to the clipboard with `pbcopy` only, never `pbpaste`.
