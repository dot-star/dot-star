---
description: Reverse a /try; move the tested (possibly tweaked) state back from root into this worktree and restore root's pre-/try edits.
---

Untry: move the tested (possibly tweaked) changes back from root into this worktree and restore root's pre-/try edits.
